'use strict';

var expect = require('chai').expect;
var sum = require('../public/javascripts/main');


describe('ajaxComplete', function(data) {
  it('Should ajax complete request', function() {
  	expect('#ajaxComplete').success.to.equal = (4);
  });
  var callback = new getJSON,
    profile1 = new User(arrData[0]),
    profile1 = new User(arrData[1]),
    profile1 = new User(arrData[2]),
    profile1 = new User(arrData[3]),
    profile1 = new User(arrData[4])

  beforeEach(function(data) {
    db.clear(function(err) {
      if (err) return done(err);
      db.save([tobi, loki, jane], done);
    });
  });

//test of break
describe('my suite', () => {
  it('my test', () => {
    // should set the timeout of this test to 1000 ms; instead will fail
    this.timeout(1000);
    assert.ok(true);
  });
});
